#!/usr/bin/env python3
"""Rocking: the oscillating rotation, as a module type of its own.

The class lives in rotation.py beside its continuous sibling; the loader
finds a module by the type name, so this file gives it one.
"""

from rotation import OscillatingRotationModule  # noqa: F401
